**# 🔍 Log Analyzer with Machine Learning

A Python-based log analysis tool that uses machine learning (Isolation Forest)
to detect anomalies in authentication logs.

## 🚀 Features
- Parses raw Linux auth logs
- Extracts security-relevant features
- Detects anomalies using Isolation Forest**
